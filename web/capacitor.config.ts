import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.regattaone.boat',
  appName: 'RegattaOne Boat',
  webDir: 'dist/regattaone-web/browser',
};

export default config;
